<%= form_for @listing do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :image_url %>
  <%= f.text_field :image_url %>

  <%= f.label :city %>
  <%= f.collection_select :city_id, City.order(:name), :id, :name %>

<% end %>
